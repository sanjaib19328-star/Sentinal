spring:
  config:
    activate:
      on-profile: prod

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    root: WARN
    com.sentinel.api: INFO
    org.springframework.security: WARN
    org.hibernate: WARN
