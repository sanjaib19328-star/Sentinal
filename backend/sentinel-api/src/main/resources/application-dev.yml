spring:
  config:
    activate:
      on-profile: dev

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

logging:
  level:
    root: INFO
    com.sentinel.api: DEBUG
    org.springframework.security: INFO

sentinel:
  security:
    ssrf:
      allow-local: true
